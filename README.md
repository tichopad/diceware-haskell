# Diceware passphrase generator in Haskell

## Requirements

- GHC 8
- GNU Make

## Build

Run `make build` in terminal.

## Run

After build, run `./dist/diceware LENGTH SEPARATOR`.

E.g.: `./dist/diceware 8 -`